import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';

export const contestsSlice = createSlice({
  name: 'contests',
  initialState: {
    
  },
  reducers: {
    
  },
  extraReducers: {
    
  },
});

export const { } = contestsSlice.actions;

export const contestsSelector = (state) => state.user;
