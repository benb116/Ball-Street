import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';

export const entrySlice = createSlice({
  name: 'entry',
  initialState: {
    
  },
  reducers: {
    
  },
  extraReducers: {
    
  },
});

export const { } = entrySlice.actions;

export const entrySelector = (state) => state.user;
