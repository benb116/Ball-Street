import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';

export const leagueSlice = createSlice({
  name: 'league',
  initialState: {
    
  },
  reducers: {
    
  },
  extraReducers: {
    
  },
});

export const { } = leagueSlice.actions;

export const leagueSelector = (state) => state.user;
